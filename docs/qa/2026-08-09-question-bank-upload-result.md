# QA 실행 결과 — 문제은행 실데이터 투입 회차 (문제_01_공통)

- **대상 파일:** [`docs/문제은행_엑셀/문제_01_공통.xlsx`](../문제은행_엑셀/문제_01_공통.xlsx) 43행
- **대상 커밋:** `616cd92` (브랜치 `fix/accessibility-d5-d6`)
- **실행일:** 2026-08-09
- **실행 방식:** 실제 화면(`/admin/problems/excel-upload`)에서 업로드 + psql 직접 조회
- **업로더:** `admin` (SUPER_ADMIN, 본사/HQ)
- **환경:** 프론트 `localhost:5173`, 백엔드 `localhost:8080`, DB `localhost:5434`

> 이 회차를 연 이유: Plan 3 QA는 `문제은행_엑셀` 파일을 **한 번도 올린 적이 없다.** 파일이 QA 종료 19분 뒤(`a8e1190`)에 저장소에 들어왔고, 두 DB 어디에도 `target_type='PROBLEM'` 업로드 이력이 없었다. §8 엑셀 항목은 전부 경계·오류 조건용 인공 데이터로만 수행됐다.

## 결과 요약

**43행 전부 성공. 실패 0건.** 정적 예측(653행 전부 통과)이 이 파일에서는 그대로 맞았다.

| 항목 | 결과 |
|---|---|
| 화면 결과 | 전체 43건 중 성공 43건 / 실패 0건 |
| 소요 시간 | 약 10초 (초당 4~5행) |
| `problems` | 13 → **56** (+43) |
| 부서 귀속 | 43건 전부 **본사(업로더 소속)** |
| 유형 분포 | MCQ_SINGLE 40 · SHORT_ANSWER 3 |

유형 분포는 변환 로그(`_검증로그.txt`)의 `업로드행=43 (객관식단일40 복수0 OX0 단답3)`와 **정확히 일치**한다.

## 정적 검사로는 확인할 수 없었던 것 — 실제로 확인한 결과

이번 회차의 목적이다. 파일을 열어 보는 것만으로는 알 수 없는 항목들이다.

### 1. 태그 통합 — 새 태그를 만들지 않고 기존 태그를 재사용한다 ✅

업로드 전 `tags` 테이블 3건(`회계`·`안전`·`공통`), 업로드 후에도 **3건 그대로**다. 엑셀의 `공통` 태그 43개가 기존 `공통`(id 40)에 붙었다.

| 태그 | 업로드 전 | 업로드 후 |
|---|---:|---:|
| 공통 | 5 | **48** |
| 회계 / 안전 | 6 / 5 | 6 / 5 |

### 2. 업로드 이력 (§8.22) ✅

```
id|target_type|department_id|file_name         |total|success|fail
13|PROBLEM    |1(본사)      |문제_01_공통.xlsx  |  43 |    43 |   0
```

`target_type='PROBLEM'`, `department_id`가 업로더 부서, 건수가 화면과 일치한다.

### 3. 감사 로그 ✅

| action | 건수 |
|---|---:|
| `PROBLEM_EXCEL_UPLOADED` | 1 |
| `PROBLEM_CREATED_BY_EXCEL` | **43** |

`detail`: `{"failRows":0,"fileName":"문제_01_공통.xlsx","totalRows":43,"successRows":43}`

### 4. 정답 플래그 왕복 — Plan 4 채점의 전제 ✅

가장 중요한 확인이다. 엑셀의 정답 번호가 `problem_choices.is_correct`로 저장되고, 상세 조회를 거쳐 수정 화면까지 살아서 돌아온다.

문제 74번(보기 4개, 정답 4번):

```
choices      : [출처 불명 문자의…, 금융·공공기관은…, 의심스러우면…, 빠른 처리를 위해…]
correctFlags : [false, false, false, true]     ← 4번째만 true
```

DB에서도 본사 문제 전부 `보기 4개 / 정답 1개`로 일관된다. 주관식도 `problem_answers`에 정답이 그대로 들어간다(예: `부취제`).

### 5. 화면 렌더링과 필터 ✅

| 확인 | 결과 |
|---|---|
| 목록 요약 | "전체 56건" · 56행 렌더링 |
| 태그 필터 `공통` | 48건 |
| 태그 `공통` + 유형 `주관식` | 5건 (AND 정상) |
| 상세(수정) 화면 왕복 | 유형·내용·태그·정답 모두 복원 |

## 발견 사항

### F1. 문제 목록에 페이지네이션이 없다

56건이 **한 화면에 전부 렌더링**된다. 653문항을 모두 투입하면 653행이 한 번에 그려진다. 기능 결함은 아니지만 전량 투입 전에 판단이 필요하다. Plan 3 체크리스트·플랜에 페이징 요구가 없어 **미구현이 의도인지 누락인지 확인이 필요하다.**

### F2. 원문 줄바꿈으로 생긴 공백이 그대로 저장된다 (기존 인지)

`"요금 및 공급조건에 관 한"`, `"무색 무취의 탄 소화합물"`처럼 단어 중간에 공백이 남는다. 변환 단계의 한계로 [README](../문제은행_엑셀/README.md)에 이미 기록돼 있다. 업로드·채점에는 영향이 없으나 **학습자에게 그대로 노출**되므로, 전량 투입 전에 엑셀에서 일괄 정리할지 결정하는 편이 낫다.

## 남은 것

| 항목 | 상태 |
|---|---|
| 나머지 11개 파일 (610행) | 미투입. 팀별 관리자 계정으로 각각 올려야 부서 격리가 맞는다 |
| 수동 입력 69문항 | 미투입. 다중빈칸 50건은 엑셀 미지원이라 개별 등록 화면 필요 |
| F1 페이지네이션 판단 | 전량 투입 전 결정 필요 |

---

## 부서 지정 기능 검증 (2026-08-09)

- **대상:** [`2026-08-09-excel-upload-department-selection.md`](../superpowers/plans/2026-08-09-excel-upload-department-selection.md) Task 8
- **방식:** Vibescraper(실제 Chrome) + psql, 뷰포트 1440×1024
- **계정:** `admin`(SUPER_ADMIN·본사) / `dev_admin`(DEPT_ADMIN·정보시스템팀)

### 총괄 관리자 — 타부서 명의 업로드 ✅

| 확인 | 결과 |
|---|---|
| Select 옵션 | 활성 부서 6개만. 비활성 2개(`OLD`, 길이 테스트 부서) 제외 |
| 초기값 | `""` — "부서 선택". 본인 부서 자동 선택 없음 |
| 부서 미선택 후 업로드 | "업로드할 문제가 귀속될 부서를 선택하세요."로 차단, 업로드되지 않음 |
| `문제_02_기획팀.xlsx` → 정보시스템팀 | **61/61 성공** (2초) |

`admin`은 **본사** 소속인데 문제 61건이 전부 **정보시스템팀**에 들어갔다.

```
정보시스템팀  10 → 71  (+61)
본사          46 → 46  (변화 없음)

excel_upload_logs.department_id = 2 (정보시스템팀)   ← 업로더 부서(1)가 아님
audit detail  {..., "departmentId": 2}              ← detail::jsonb 파싱 성공
```

문제 행과 업로드 이력이 **같은 값**을 쓰는 것이 실물로 확인됐다.

### 부서 관리자 — 고정 ✅

| 확인 | 결과 |
|---|---|
| Select | `disabled: true`, 옵션 1개("정보시스템팀"), 값 `"2"` |
| 안내 문구 | "소속 부서로만 등록할 수 있습니다." |
| 콘솔 403 | **없음** — 부서 목록 API를 호출하지 않는다 |
| 수정 화면의 부서 이동 카드 | **비노출** |

### 파라미터 위조 차단 ✅ (보안)

`dev_admin` 세션으로 `?departmentId=1`(본사)을 붙여 직접 호출했다. 요청은 200으로 성공했지만 **저장은 본인 부서로** 됐다.

```
DEV  71 → 73  (+2)   ← 위조한 값이 아니라 본인 부서
HQ   46 → 46  (변화 없음)
excel_upload_logs.department_id = 2
```

부서 이동 API도 `dev_admin`으로 직접 호출해 **403 `{"resultCode":990,"resultMsg":"접근 권한이 없습니다."}`** 를 확인했다. 문제의 `department_id`는 바뀌지 않았고 감사 로그도 남지 않았다.

### 부서 이동 ✅

`admin`으로 문제 172번을 정보시스템팀 → 영업팀으로 옮겼다.

```
problem 172  department_id 2 → 4 (SALES)
problem 173  department_id 2    (영향 없음)
audit detail {"to": 4, "from": 2}   ← detail::jsonb 파싱 성공
```

## 이 회차에서 잡은 결함 2건 (둘 다 구현 중 만든 것)

### E1. PATCH 요청이 CORS 에서 거부됐다

부서 이동 API를 `@PatchMapping`으로 만들었는데 `CorsConfig:28`의 `allowedMethods`가 `GET/POST/PUT/DELETE/OPTIONS` 뿐이라, 컨트롤러에 닿기도 전에 **403 `Invalid CORS request`** 로 막혔다. 부서 관리자만이 아니라 **총괄 관리자의 부서 이동도 동작하지 않는 상태**였다.

`PUT /{id}/department`로 바꿔 해결했다. CORS 허용 목록을 넓히는 대신 이 저장소가 일관되게 쓰는 동사에 맞췄다 — PATCH를 쓴 전례가 없고, 부서 배정이라는 하위 리소스를 통째로 교체하는 의미라 PUT이 맞다.

> 서비스를 직접 호출하는 단위 테스트로는 잡을 수 없는 종류다. 처음 받은 403을 "권한 차단 성공"으로 오독할 뻔했는데, 응답 본문이 `Invalid CORS request`라 드러났다.

### E2. React Hooks 순서 위반으로 수정 화면이 통째로 깨졌다

부서 목록을 받는 `useEffect`를 조기 return(`loading`/`permissionDenied`/`loadError`) **아래**에 두어, 첫 렌더에서는 실행되지 않고 로딩이 끝난 뒤에야 실행됐다. 훅 개수가 렌더마다 달라져 `Rendered more hooks than during the previous render`로 ErrorBoundary에 떨어졌다.

```
console: React has detected a change in the order of Hooks called by ProblemFormPage
console: Error: Rendered more hooks than during the previous render.
```

훅을 조기 return 위로 옮기고 그 이유를 주석으로 고정했다.

> 프로덕션 빌드와 184개 테스트는 모두 통과한 상태였다. jsdom이 없어 컴포넌트 렌더링 테스트를 못 하는 이 프로젝트에서는 **브라우저로 열어 보는 것 말고 이 결함을 잡을 방법이 없다.**

---

## F1 해소 + 목록 잘림 수정 검증 (2026-08-09)

- **대상:** [`2026-08-09-admin-list-scroll-and-pagination.md`](../superpowers/plans/2026-08-09-admin-list-scroll-and-pagination.md) Task 5
- **방식:** Vibescraper(실제 Chrome) + API 직접 호출 + psql
- **데이터:** 문제 119건 (본사 46 · 정보시스템팀 72 · 영업팀 1)

### 목록 카드 잘림 — 해소 ✅

`AppShell`이 `h-screen` + `overflow-hidden`으로 화면 높이에 고정돼 **문서가 아예 스크롤하지 않고 `main`만 내부 스크롤**했다. 그래서 창이 조금만 짧아도 카드가 하단 테두리 없이 끊겨 렌더링 오류처럼 보였다. **행 수와 무관한 레이아웃 문제**였다 — 8행짜리 부서 목록도 창 높이 800px에서 잘렸다.

문서 스크롤로 되돌리고 Sidebar·Topbar에 `sticky`를 걸었다. 창 높이 4종 × 화면 3종 **12조합 전부 통과**한다.

| 창 높이 | 부서 | 계정 | 문제 |
|---|---|---|---|
| 720 | 688 ≤ 720 ✅ | 688 ≤ 720 ✅ | 632 ≤ 720 ✅ |
| 800 | 768 ≤ 800 ✅ | 768 ≤ 800 ✅ | 712 ≤ 800 ✅ |
| 900 | 868 ≤ 900 ✅ | 854 ≤ 900 ✅ | 812 ≤ 900 ✅ |
| 1024 | 902 ≤ 1024 ✅ | 854 ≤ 1024 ✅ | 936 ≤ 1024 ✅ |

가로 스크롤은 전 조합에서 없다. 1500px 스크롤 후에도 Sidebar(`top: 0`)와 Topbar(`top: 0`)가 화면에 남는다.

### 페이지네이션 ✅

| 확인 | 결과 |
|---|---|
| 문제 목록 초기 | 20행 표시, "1–20 / 전체 119건", `1 / 6` |
| 다음 페이지 | "21–40 / 전체 119건", `2 / 6`, 20행 |
| 필터 후 조회 | 2페이지에 있던 상태에서 유형=OX 조회 → **전체 4건, 4행** (1페이지부터 재조회됨) |
| 부서 목록(8건) | 페이지 컨트롤 **비노출** |
| 계정 목록(7건) | 페이지 컨트롤 **비노출** |

### 페이징 순서의 결정성 ✅ — 중복·누락 없음

계획 검토 단계에서 `ORDER BY p.created_at DESC`만으로는 전순서가 아니라는 위험을 발견해 `p.id DESC` 타이브레이커를 추가했다. 실측으로 확인했다.

```
6개 페이지를 이어 붙인 결과: 119건 수집 / 고유 119건  → 중복 0, 누락 0
API 순서 앞 30개 == SELECT id FROM problems ORDER BY created_at DESC, id DESC 앞 30개  → 일치
```

DAO 테스트에도 `created_at`을 모두 같은 값으로 맞춘 뒤 페이지를 이어 붙이는 케이스를 넣어 회귀를 막았다.

### 모달이 sticky Topbar에 가리지 않는다 ✅

| 요소 | z-index |
|---|---|
| Modal 오버레이 | **50** |
| Topbar | 20 |

부서 수정 모달을 열어 확인했다. 모달이 Topbar 위에 온다.

### F1 판정

**해소.** 문제 목록은 서버가 20건씩 잘라 주므로 653문항을 전량 투입해도 한 화면에 653행이 그려지지 않는다. `size` 상한 100을 서버가 강제한다.
