# 랜덤 풀이·필터 개선 브라우저 검증 결과 (Task 9)

- 일시: 2026-08-12
- 대상: 브랜치 `feat/solve-random-and-filter-fixes` (`7977ab6..99a215a`)
- 환경: Docker Postgres 5434 / 백엔드 8080(`MAIL_PORT=1025`) / Vite 5173
- 계정: `emp001` / `QaPlan3!2026` (직원·영업팀), `admin` / `QaAdmin1234!` (총괄관리자)

**판정: 전 항목 통과. 결함 0건.** 전 과정에서 브라우저 콘솔 오류·경고 **0건**.

---

## 1. API 계층 (화면 우회 직접 호출)

| 확인 | 결과 |
|---|---|
| `GET /api/departments` | 활성 3개(본사·개발팀·영업팀). **비활성 폐지팀 미포함** ✅ |
| `GET /api/tags/in-use` | 활성 문제 기준으로 좁혀짐. **활성 문제 0건이던 `과학`·`기초` 사라짐** ✅ |
| `GET /api/problems/random?count=4` × 3회 | `[254,235,230,229]` / `[229,228,231,235]` / `[229,231,233,234]` — **매번 다름** ✅ |
| `count=0` | `resultCode 1000` "문제 수는 1 이상 50 이하여야 합니다." ✅ |
| `count=51` | 동일하게 거부 ✅ |
| 부서 필터 | 개발팀 8건 · 영업팀 2건 · 본사 0건 · 미지정 10건 — 정확히 좁힘 ✅ |

### D1 확인 — 부서는 접근 제한이 아니라 사용자 선택

`emp001`은 **영업팀** 소속인데 `departmentId=862`(개발팀)로 요청하니 **개발팀 문제 8건이 정상 반환**됐다. 자기 부서로 제한되지 않는다 — 확정 결정 D1대로다.

---

## 2. 랜덤 풀이 전 과정

| 단계 | 확인 | 결과 |
|---|---|---|
| 학습 홈 | 카드 3개 — 랜덤으로 풀기 / **골라서 풀기** / 내 풀이 이력 | ✅ |
| 설정 화면 | 문제 수 기본 `10문제`, 부서 기본 **`전체 부서`**(사용자 부서 자동선택 없음) | ✅ D1 |
| 부서 목록 | 본사·개발팀·영업팀. **폐지팀(비활성) 없음** | ✅ |
| 세트 시작 | 5문제·개발팀 → `/solve/random/play`, 진행률 `1 / 5` | ✅ |
| 제출 | 채점 결과 표시("정답입니다!"), **"다음 문제" 버튼만 노출** | ✅ |
| 자동 전환 없음 | 버튼을 눌러야 넘어감 — 채점 결과 확인 시간 보장 | ✅ |
| 다음 문제 | `2 / 5`로 진행, 앞 문제 답 잔존 없음 | ✅ |
| 끝까지 | `/solve/random/result` → **"5문제 중 3개 정답"** | ✅ |
| 결과 화면 행동 | "다시 랜덤으로 풀기" · "학습 홈으로" 2개 | ✅ |

### 세트 이탈구가 사라졌다

리뷰에서 잡힌 결함(`ProblemSolveCard`가 채점 후 "다른 문제 풀기" 링크를 스스로 렌더 → 세트를 조용히 이탈)이 수정됐다.

- **세트 진행 화면:** 채점 후 링크 `["문제 은행 Hub"]` — 이탈구 없음 ✅
- **단건 풀이 화면:** 채점 후 `["문제 은행 Hub","문제 목록","다른 문제 풀기"]` — 기존 동작 유지 ✅
- 채점 **전**에는 단건 화면에도 안 나옴 ✅

---

## 3. 세션 방어 (D4)

| 조작 | 기대 | 결과 |
|---|---|---|
| 진행 중 새로고침 | 같은 위치에서 이어짐 | `2 / 5` 유지 ✅ |
| 결과 화면 도달 후 sessionStorage | 지워짐 | `null` ✅ |
| 결과 화면에서 뒤로 가기 | 다 푼 세트로 안 돌아감 | 설정 화면 ✅ |
| 세션 없이 `/play` 직접 접근 | 설정 화면으로 | ✅ |
| JSON이 깨진 세션 | 설정 화면으로 | ✅ |
| 형태가 틀린 세션(`problemIds`가 배열 아님) | 설정 화면으로 | ✅ |

빈 화면이나 오류로 끝나는 경로가 하나도 없었다. 무한 리다이렉트도 없었다.

---

## 4. 0건 처리

본사(문제 0건)를 골라 시작:

- 안내 문구 **"조건에 맞는 문제가 없습니다. 부서나 문제 수를 바꿔 보세요."** 노출
- **이동하지 않고** 설정 화면 유지
- sessionStorage `null` — 빈 세션이 만들어지지 않음

---

## 5. 태그 선택지 (D8)

정리 **전**: `개발 · 엑셀 · 지리 · 태그 · 네트워크 · 수정태그` (`과학`·`기초`는 API 단계에서 이미 제외됨)

QA 잔여 태그를 **화면에서** 제거했다(DB 직접 수정 아님, 감사 로그 남음):

| 문제 | 조치 |
|---|---|
| 228 `수정된 OX 문제 본문` | 태그 `수정태그` 제거 |
| 233 `엑셀 정상1` | 태그 `태그` 제거(`엑셀`은 유지) |

정리 **후** 최종 선택지와 각 결과 건수:

| 태그 | 결과 |
|---|---|
| 개발 | 2건 |
| 엑셀 | 4건 |
| 지리 | 1건 |
| 네트워크 | 1건 |

**고르면 0건이 나오는 선택지가 하나도 없다** — 이 항목의 완료 기준 충족.

---

## 6. 스타일 (D10)

### 빈칸 입력칸

`getComputedStyle` 실측: `textAlign: center` ✅, `width: 112px`(=`w-28` 유지) ✅

폭을 고정으로 둔 것은 의도다 — 정답 길이에 맞춰 늘리면 글자 수가 노출돼 힌트가 된다.

### Select 화살표 — 공용 컴포넌트 회귀 확인

`Select.jsx`는 공용이라 관리자 화면 전체가 함께 바뀐다. **바뀌는 화면을 전부 실측했다.**

| 화면 | Select | appearance | 좌패딩 | 우패딩 | 화살표~우측 테두리 | 겹침 |
|---|---|---|---|---|---|---|
| 풀이 목록 | `solve-tag` | none | 12px | 36px | 12px | 없음 |
| 부서 관리 | `department-status-filter` | none | 12px | 36px | 12px | 없음 |
| 계정 관리 | `user-status-filter` | none | 12px | 36px | 12px | 없음 |
| 계정 관리 | `user-department` | none | 12px | 36px | 12px | 없음 |
| 계정 관리 | `user-role` | none | 12px | 36px | 12px | 없음 |
| 문제 관리 | `problem-type-filter` | none | 12px | 36px | 12px | 없음 |
| 문제 관리 | `problem-status-filter` | none | 12px | 36px | 12px | 없음 |
| 문제 등록 | `problem-type` | none | 12px | 36px | 12px | 없음 |
| 문제 등록 | `problem-create-department` | none | 12px | 36px | 12px | 없음 |

9개 전부 동일. 좌측 12px(디자인 시스템 §7.5) 유지, 화살표가 테두리에 붙지 않고 12px 떨어짐, `pointer-events: none`이라 아이콘을 눌러도 select가 열린다.

---

## 7. 이력·모바일·콘솔

| 확인 | 결과 |
|---|---|
| 이력 건수 | 화면 13건 = API `/api/attempts/me` 13건 — 일치 ✅ |
| 정답/오답 표시 | 실제 채점과 일치 ✅ |
| 모바일(390×844) 학습 홈 | 3갈래 정상, 가로 스크롤 없음 ✅ |
| 모바일 랜덤 설정 | 정상 동작, 오버플로 없음 ✅ |
| **브라우저 콘솔** | 전 과정 **오류·경고 0건** ✅ |

---

## 8. 범위 밖으로 확인만 한 것

**이력 화면이 빈칸 문제 본문을 마커 그대로 보여준다.**

```
예산의 3요소는 {{b1}}, {{b2}}, {{b3}} 이다.
```

`docs/qa/2026-08-12-plan4-solve-qa-manual.md` §7.1에서 예상했던 항목이다. **이번 브랜치가 만든 것이 아니라 Plan 4부터의 기존 동작**이라 범위에서 제외했다. 풀이 목록에도 같은 현상이 있다. 학습자에게 코드가 노출되는 문제이므로 별도 과제로 다룰 가치가 있다.

---

## 9. 테스트

```
백엔드   ./gradlew test   → 230 통과
프론트   npm test         → 232 통과
빌드     npm run build    → 성공, 경고 없음
```

**단, 이 브랜치의 화면 작업은 자동 검증이 0이다.** jsdom이 없어 `ProblemSolveCard`·`RandomSetupPage`·`RandomPlayPage`·`RandomResultPage`·`Select` 변경분을 테스트가 한 줄도 실행하지 않는다. 위 브라우저 검증이 그 공백을 메우는 유일한 수단이다.
